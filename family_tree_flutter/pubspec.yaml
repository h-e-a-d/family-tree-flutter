name: family_tree_flutter
description: A Flutter family‐tree builder with connectors, JSON export/import, font pickers, undo, and “bring to front.”

publish_to: "none"

environment:
  sdk: ">=2.18.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # No additional plugins required for the features below.

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
